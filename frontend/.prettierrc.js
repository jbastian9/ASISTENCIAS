module.exports = {
  // Poner punto y coma al final de cada línea
  semi: true,
  // Usar comillas simples en vez de dobles
  singleQuote: true,
  // Comas al final de objetos y arrays en ES5 compatible
  trailingComma: 'es5',
  // Largo máximo de línea antes de hacer wrap
  printWidth: 80,
  // Tamaño de tabulación (2 espacios)
  tabWidth: 2,
  // No usar tabs, usar espacios
  useTabs: false,
  // Espacios entre llaves { foo: bar }
  bracketSpacing: true,
  // Siempre poner paréntesis en argumentos de arrow functions
  arrowParens: 'always',
  // Saltos de línea tipo LF (evita conflictos en Windows)
  endOfLine: 'lf',
}